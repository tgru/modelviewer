class ModelRenderer {
    // build scene and canvas with given size
    constructor(width, height) {}

    // load model
    load(path) {}

    // release resources
    destroy() {}

    // return canvas
    getCanvas() {}

    // resize canvas
    resize(width, height) {}
}